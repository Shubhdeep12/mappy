/**
 * LLM Provider Interface Re-export
 * 
 * Re-exports the LLM provider interface from shared package.
 * Backend implementations will implement this interface.
 */

export type { LLMProvider, GenerateOptions, JSONSchema } from '@mappy/shared';
