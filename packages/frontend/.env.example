# =============================================================================
# Frontend – copy to packages/frontend/.env if you need to override the API URL.
# Local dev: Vite proxies /api to backend; often no .env needed.
# Production (e.g. Vercel): set VITE_API_BASE_URL to your backend URL.
# =============================================================================

# VITE_API_BASE_URL=http://localhost:8080/api
