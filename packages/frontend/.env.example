# Frontend – copy to .env if you need to override the API base URL.
# With Vite dev server, /api is proxied to the backend; no env required for local dev.

VITE_API_BASE_URL=http://localhost:8080/api
