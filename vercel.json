{
  "buildCommand": "pnpm run build:shared && pnpm run build:frontend",
  "outputDirectory": "packages/frontend/dist",
  "installCommand": "corepack enable && pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
